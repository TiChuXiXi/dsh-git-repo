# 动态热加载实测记录

这个文件是**临时验证物**：为测试 `gitvc-1` 动态插件的「未跟踪文件 → 暂存 → 提交」链路而新建。

- 创建方式：聊天里直接写盘（UTF-8，无 BOM）
- 期望在面板里的表现：Local Changes 的「未跟踪文件」分组出现 `LIVE-CHECK.md`，状态字母 `?`（绿色）
- 验证完成后可提交留档，也可在面板里直接删掉这个文件

配套的已跟踪文件改动：`README.md`（新增「动态热加载验证版」小节），状态字母 `M`（蓝色）。
